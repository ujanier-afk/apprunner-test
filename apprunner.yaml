version: 1.0
runtime: python311

build:
  commands:
    build:
      - pip install --upgrade pip
      - pip install -r requirements.txt

run:
  command: uvicorn app:app --host 0.0.0.0 --port 8080
  network:
    port: 8080
